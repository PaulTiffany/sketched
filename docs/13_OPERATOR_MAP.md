# 13 · Operator map — engineering operators traced to mathematical operators

**Generated by `verification/tools/operator_audit.py` from
[`../verification/operators.json`](../verification/operators.json) — edit the JSON, not this file.**
Every `realized` row's code reference is mechanically verified on each suite run;
`target` rows are design intent and are checked to claim no code. Paper references
are to `forcing_correspondence_v15.tex` (atlas node ids).

## Zero-order design principles (human-labelled calibration data)

| Label | Mathematical operator | Engineering operator | Code |
|---|---|---|---|
| **localhost** | the void 1̂: unique shared depth-0 condition (lem:void, prop:zeroth) (`lem:void`) | dev server bound to 127.0.0.1; ZeroOrderAnchor origin check | `src/witness/witness.ts#isZeroOrderOrigin` |
| **browser-platform** | interface medium M through which all transfer factors (`thm:factor`) | the browser as public medium: file ingestion, JSON artifacts, rendering | `src/witness/residue.ts#Mark` |
| **json-manifold** | encoded residue E_A(T_A x): what actually crosses the medium (`thm:nonid`) | Mark as JSON-serializable residue schema | `src/witness/residue.ts#encodeResidue` |
| **eula-first-contract** | seating the human at the depth-0 origin (normative move; docs/12) (`prop:zeroth`) | EULA acceptance recorded in ZeroOrderAnchor before any trace validates | `src/witness/witness.ts#anchor` |

## Operators

| Math (atlas id) | Mathematical operator | Engineering operator | Code | Cal. item | Status |
|---|---|---|---|---|---|
| `def:cond` | condition p: finite partial epistemic state (claim complex K_t) | Stage state: the layer set + timeline at a tick | `src/core/stage.ts#Stage` | — | realized |
| `def:refine` | admissible refinement q ⊑ p retaining commitments | gatekeeper-accepted Proposal application | `src/agents/gatekeeper.ts#Gatekeeper` | 2 | realized |
| `def:wit` | J_wit finite witness: refinement prefix with certificate, admissibility, provenance, human gate | WitnessTrace of WitnessedSteps bound from Proposal/ConsentDecision/ProposalResult | `src/witness/witness.ts#WitnessTrace` | 2 | realized |
| `def:wit` | certificate carried by each witnessed refinement | ConsentDecision (required/granted/reason) | `src/core/consent.ts#ConsentDecision` | 2 | realized |
| `def:wit` | human-gated decision (where required) | ManualConsentPolicy.grant: explicit per-proposal human approval | `src/core/consent.ts#ManualConsentPolicy` | 2 | realized |
| `thm:nonid` | channel projector T_A (bounded projection; identity breaks here first) | explicit field whitelist applied before export | `src/witness/residue.ts#project` | 10 | realized |
| `thm:nonid` | encode E_A: floor-stable residue onto the medium | encodeResidue: state → Mark (JSON), loss declared | `src/witness/residue.ts#encodeResidue` | 10 | realized |
| `thm:nonid` | decode D_B: reconstruction inside the reader's surface, never identity | decodeResidue: Mark → Reconstruction (nominally branded type; not source state) | `src/witness/residue.ts#decodeResidue` | 10 | realized |
| `prop:chalked` | Chalked correctness invariant: provenance, permission, residue type, revision authority; identity refused | validateMark verdicts | `src/witness/validate.ts#validateMark` | 10 | realized |
| `def:wit` | J_wit trace validity conditions | validateTrace verdicts (anchor + per-step certificate/admissibility coherence) | `src/witness/validate.ts#validateTrace` | 2 | realized |
| `lem:void` | witness / certificate ledger along the trace | append-only AuditLog | `src/core/provenance.ts#AuditLog` | 2 | realized |
| `prop:chalked` | revision authority (the eraser) | shake as scoped revocation | `src/core/shake.ts#ShakeScope` | — | realized |
| `asm:calibration` | non-actuation boundary: witness and request only, no autonomous loop closure | agents propose, the surface disposes; no agent mutates core state | `src/agents/agentProtocol.ts#Proposal` | — | realized |
| `lem:margin` | path-form margin: anchor minus cumulative drift remains above the interval floor | Flow channel meters derived from anchor, cumulative event cost, and finite budget | `src/flow/engine.ts#channelMeters` | — | realized |
| `def:refine` | bounded admissible refinement within a channel-margin interval | FlowChannel: finite lease, angle, cumulative budget, and explicit closure | `src/flow/engine.ts#FlowChannel` | 8 | realized |
| `def:wit` | finite witnessed refinement prefix | exported flow trace replay, invariant validation, and final-state digest | `src/flow/engine.ts#verifyTrace` | 2 | realized |
| `prop:chalked` | revision authority follows dependent residue while preserving the authority-origin | human interrupt closes flow, rewinds its root, and removes its dependency closure | `src/flow/engine.ts#interrupt` | — | realized |
| `def:stab` | Stab(q, φ): atomic stabilization under admissible refinement | a contribution 'holds' when its stabilizers are witnessed and consented (interpretation; not shipped) | — | 1 | target |
| `def:clauses` | ∧ connective realization: certificate combination | precedence / conflict arbitration between overlapping proposals (docs/11) | — | 4 | target |
| `def:clauses` | ∨ connective realization: admissible-cover branching | branching proposals over an undecided region (docs/11; arbitration's dual) | — | 5 | target |
| `rem:torsion` | torsion certificate: refusal stable under EVERY refinement (atomic case, discharged) | the human-layer guard inside every ConsentPolicy: 'consent cannot be granted' — permanent and structural, unlike a contingent denial awaiting consent | `src/core/consent.ts#ConsentPolicy` | 6 | realized |
| `lem:cauchy` | observer completion (P_O, d_O): Cauchy-generic limit of the session | session persistence / timeline completion (Principia interface; outside Sketched's scope) | — | 9 | target |
| `def:req` | admissible dense requirement D ∈ D_adm (obligation the observer must eventually meet) | standing obligation queue for the stage (not designed yet) | — | 3 | target |
| `def:refine` | channel-margin subposet P_H^η: the admissible region + authority to move in it | Channel grant / angular access (docs/11); exit = return to zero-point | — | 8 | target |
| `asm:smooth` | control forcing / forced integration at the conflict threshold | two-knob coordinated co-motion authorizing new content (docs/11) | — | 7 | target |

## Reading the statuses

- **realized** — the code exists and exports the named symbol (checked).
  This is S-status in the paper's ledger sense: implementation conformance,
  not proof that the operation realizes the Kripke–Joyal clause it mirrors.
- **target** — design intent from docs/11–12. Presenting these as shipped
  would be exactly the ledger dishonesty the paper forbids; the auditor
  fails the build if a target row acquires a code ref without being
  promoted to realized.

The calibration queue (paper §6, Assumption 2) is the bridge debt: items
with operator entries are *addressed*, not discharged. Discharging any
item means showing the engineering operator satisfies the mathematical
operator's clause — the open (C) of the correspondence.
